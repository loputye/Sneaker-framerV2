export interface MainProps {
  /**
   * Variant
   * Friendly names map to internal IDs:
   *   "Desktop" → Bjd8x0K7L
   *   "Phone" → N7s6lX5fY
   *   "Tablet" → O5KrCuD3X
   */
  variant?: 'Desktop' | 'Phone' | 'Tablet' | 'Bjd8x0K7L' | 'O5KrCuD3X' | 'N7s6lX5fY';
  /**
   * Scroll Section — pass as `jnVwx1Al4` not `scrollSection`.
   */
  jnVwx1Al4?: string;
  /**
   * Scroll Section 2 — pass as `a5ugPOreM` not `scrollSection2`.
   */
  a5ugPOreM?: string;
  /**
   * Scroll Section 3 — pass as `FzNCaJVDs` not `scrollSection3`.
   */
  FzNCaJVDs?: string;
  /**
   * Scroll Section 4 — pass as `zq3XOjnWq` not `scrollSection4`.
   */
  zq3XOjnWq?: string;
  /**
   * Scroll Section 5 — pass as `gynpJXZmu` not `scrollSection5`.
   */
  gynpJXZmu?: string;
  /**
   * Scroll Section 6 — pass as `Lo2BbfhWO` not `scrollSection6`.
   */
  Lo2BbfhWO?: string;
  /**
   * Scroll Section 7 — pass as `DhAAQKq0D` not `scrollSection7`.
   */
  DhAAQKq0D?: string;
  /**
   * Scroll Section 8 — pass as `nP1kE_IfG` not `scrollSection8`.
   */
  nP1kE_IfG?: string;
  /** Additional properties */
  [key: string]: unknown;
}

import type * as React from 'react';

declare const MainShipping: React.ForwardRefExoticComponent<
  MainProps & React.RefAttributes<HTMLDivElement>
>;

export default MainShipping;
