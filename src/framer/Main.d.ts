export interface MainProps {
  /**
   * Variant
   * Friendly names map to internal IDs:
   *   "Desktop" → fbTzNFdr6
   *   "Phone" → SOiGtDnUd
   *   "Tablet" → xg9OZYI7V
   */
  variant?: 'Desktop' | 'Phone' | 'Tablet' | 'fbTzNFdr6' | 'xg9OZYI7V' | 'SOiGtDnUd';
  /**
   * Scroll Section — pass as `aeBO7EprP` not `scrollSection`.
   */
  aeBO7EprP?: string;
  /**
   * Scroll Section 2 — pass as `SNS4SkUw5` not `scrollSection2`.
   */
  SNS4SkUw5?: string;
  /**
   * Scroll Section 3 — pass as `PLM6pDD3g` not `scrollSection3`.
   */
  PLM6pDD3g?: string;
  /**
   * Scroll Section 4 — pass as `Buo45cCnJ` not `scrollSection4`.
   */
  Buo45cCnJ?: string;
  /**
   * Scroll Section 5 — pass as `nlIjoLLjJ` not `scrollSection5`.
   */
  nlIjoLLjJ?: string;
  /**
   * Scroll Section 6 — pass as `EaZ7wU_hC` not `scrollSection6`.
   */
  EaZ7wU_hC?: string;
  /**
   * Scroll Section 7 — pass as `qNW2O0zZ9` not `scrollSection7`.
   */
  qNW2O0zZ9?: string;
  /**
   * Scroll Section 8 — pass as `tcWX04z34` not `scrollSection8`.
   */
  tcWX04z34?: string;
  /**
   * Scroll Section 9 — pass as `rAPQODkAI` not `scrollSection9`.
   */
  rAPQODkAI?: string;
  /**
   * Scroll Section 10 — pass as `ZGavk5Rac` not `scrollSection10`.
   */
  ZGavk5Rac?: string;
  /**
   * Scroll Section 11 — pass as `IZtY6aT9u` not `scrollSection11`.
   */
  IZtY6aT9u?: string;
  /** Additional properties */
  [key: string]: unknown;
}

import type * as React from 'react';

declare const Main: React.ForwardRefExoticComponent<
  MainProps & React.RefAttributes<HTMLDivElement>
>;

export default Main;
